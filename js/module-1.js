const magicBtn = document.querySelector('.js-magic-btn');

magicBtn.addEventListener('click', () => {});

const navItemRef = document.querySelector('.site-nav');
console.log(navItemRef);

const navLinkRef = navItemRef.querySelectorAll('.site-nav__link');
console.log(navLinkRef);
